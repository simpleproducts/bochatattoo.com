// Add an image slug here to hide it from the entire site:
// - It disappears from the Work page grid and category index
// - It is removed from category counts and the total count
// - It is excluded from the homepage featured strip
// - Direct deep-links (e.g. /work?tattoo=slug) won't open it
//
// The slug is the filename (without extension) as it appears in
// src/data/images.json. One slug per line, lowercase.
export const HIDDEN_SLUGS: readonly string[] = [
  // "example-slug",
];
